import React from 'react';
import {Text, View} from 'react-native';

const InitScreen = () => {
  return (
    <View className={'flex-1 items-center justify-center'}>
      <Text className="text-green-500">Init Screen</Text>
    </View>
  );
};

export default InitScreen;
